<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalwork.dao.UserMapper">
    <select id="getById" resultType="UserDo">
        select id, name, email, password  from user where is_deleted = 0 and id = #{id};
    </select>
    <select id="getByName" resultType="UserDo">
        select id, name, email, password  from user where is_deleted = 0 and name = #{name};
    </select>
    <insert id="insert" parameterType="UserDo">
        insert into user (name, email, password,gmt_create) values (#{name}, #{email}, #{password},now());
    </insert>
    <update id="update" parameterType="UserDo">
        update user
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            gmt_modified = now()
        </set>
        where id = #{id};
    </update>
    <delete id="delete">
        update user set is_deleted = 1 where id = #{id};
    </delete>

</mapper>